#ifndef FRACTION_H
#define FRACTION_H

typedef struct Fraction {
	int numerator;
	int denominator;
} fract;

/*typedef struct Fraction {
	int numerator;
	int denominator;
} fract;

int fractGcd(fract a);
fract addFraction(fract a, fract b);*/

#endif
